deployment:
  kind: Deployment
  replicas: 3

service:
  enabled: true
  spec:
    type: NodePort

ports:
  web:
    nodePort: 30080

  websecure:
    nodePort: 30443

  traefik:
    expose:
      default: false

#tlsStore:
#  default:
#    defaultCertificate:
#      secretName: default-tls-cert

ingressClass:
  enabled: true
  #isDefaultClass: true
  name: traefik

#providers:
#  kubernetesIngress:
#    enabled: true
#    publishedService:
#      enabled: true
#  kubernetesCRD:
#    enabled: true

logs:
  general:
    level: INFO
  access:
    enabled: true

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 256Mi
