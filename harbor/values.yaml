expose:
  type: ingress
  tls:
    enable: true
    certSource: none
  ingress:
    hosts:
      core: registry.unixworld.io
persistence:
  enabled: true
  persistentVolumeClaim:
    jobservice:
      jobLog:
        accessMode: ReadWriteMany
